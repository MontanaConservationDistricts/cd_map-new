.popup-content img {
  float: left;
  margin-right: 10px;
  width: 70px;
  height: auto;
  border-radius: 4px;
  object-fit: contain;
}
.popup-content h3, 
.popup-content div {
  overflow: hidden; /* ensures text doesn't overlap floated image */
}
